[project]
name = "autotest-framework"
version = "1.0.0"
description = "通用自动化测试框架"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "smoke: 冒烟测试",
    "regression: 回归测试",
]